@using Tutoronic.Models
@{ Layout = "~/Views/Shared/_Layout.cshtml";
    Model1 db = new Model1();
Student s = new Student();

    if (Session["studentloging"] != null){

        s = (Student)Session["studentloging"];}
    }








<h2>@s.student_name</h2>
@foreach (var item in db.OrderDetails.Where(x => x.Order.Student_fid==s.Student_id)) { 

    <h3>@item.Cours.course_name</h3>

}